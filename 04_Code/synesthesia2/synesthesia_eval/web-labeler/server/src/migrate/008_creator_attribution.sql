ALTER TABLE clips
  ADD COLUMN IF NOT EXISTS creator_name       VARCHAR(255),
  ADD COLUMN IF NOT EXISTS creator_url        VARCHAR(512),
  ADD COLUMN IF NOT EXISTS claimed_by_user_id INTEGER REFERENCES users(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS claimed_at         TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS display_credit     TEXT,
  ADD COLUMN IF NOT EXISTS display_link       VARCHAR(512),
  ADD COLUMN IF NOT EXISTS credit_visible     BOOLEAN DEFAULT TRUE;
